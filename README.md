## React-Essentials-final

- created this as a part of react bootcamp
- using vite as a build process
- uses state to dynamically reder date

`npm install` - to install dependencies

`npm run dev` - to run the project in localhost